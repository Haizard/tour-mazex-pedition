.highlighted-products-wrapper {
    text-align: center;
    padding: 40px 20px;
  }
.highlighted-products-title {
    font-size: 32px;
    font-weight: bold;
    margin-bottom: 30px;
    color: #333;
  }
.highlighted-products-section {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
  }
.highlighted-product {
    text-decoration: none;
    position: relative;
    width: 260px;
    height: 400px;
    border-radius: 16px;
    overflow: hidden;
    background-size: cover;
    background-position: center;
    transition: transform 0.4s ease;
    color: inherit;
  }
.highlighted-product::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.2);
    transition: background 0.4s ease;
    z-index: 0;
  }
.highlighted-product:hover {
    transform: scale(1.02);
  }
.highlighted-product:hover::before {
    background: rgba(0, 0, 0, 0.4);
  }
.highlighted-product-badge {
    position: absolute;
    top: 12px;
    left: 82px;
    width: 70px;
    height: 70px;
    object-fit: contain;
    z-index: 2;
  }
.highlighted-product-title {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 20px;
    font-weight: bold;
    color: #fff;
    z-index: 2;
    text-shadow: 1px 1px 6px rgba(0, 0, 0, 0.8);
    text-align: center;
    padding: 0 10px;
  }
.highlighted-product-price {
    position: absolute;
    bottom: 12px;
    right: 12px;
    z-index: 2;
    text-align: right;
  }
.highlighted-product-price .from {
    font-size: 12px;
    color: #fff;
  }
.highlighted-product-price .price {
    font-weight: bold;
    font-size: 18px;
    color: #fff;
    border: 2px solid #fff;
    padding: 4px 10px;
    border-radius: 8px;
    display: inline-block;
    margin-top: 4px;
    transition: all 0.3s ease;
  }
.highlighted-product-price .price:hover {
    transform: scale(1.05);
    box-shadow: 0 0 6px 2px rgba(255, 255, 255, 0.6);
  }
.highlighted-products-section {
      flex-direction: column;
      align-items: center;
    }
.highlighted-product {
      width: 90%;
      height: 350px;
    }
.highlighted-products-title {
      font-size: 24px;
    }
.highlighted-product-badge {
      top: 12px;
      left: 50%;
      transform: translateX(-50%);
    }